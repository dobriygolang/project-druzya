---
description: Go backend conventions for recommendation service
globs: "**/*.go"
alwaysApply: false
---

# Go backend — recommendation

Transport thin; logic in `internal/recommendation/service/`. One RPC per file in `internal/app/api/recommendation/`.

Domain writes in handlers use `Repository.WithTx`.
