---
description: interview service — architecture and workflow
alwaysApply: true
---

# interview service

Module: `github.com/sedorofeevd/project-druzya/services/interview`. Work from service root only.

## Layers

```
cmd/interview/app/run.go     → DI
internal/app/api/identity/ → gRPC transport
internal/interview/          → model, repository, service
internal/adapter/postgres/  → persistence
```

## Dev

`make start` | `make run` | `make lint` | `make gen-proto`

HTTP `:8082`, gRPC `:9092`. See `AGENTS.md`.
