---
description: Go backend conventions for interview service
globs: "**/*.go"
alwaysApply: false
---

# Go backend — interview

- Domain: `internal/interview/service/` — `Service` interface
- Transport: `internal/app/api/interview/` — JWT auth, internal token for evaluation
- Content via `internal/adapter/content/` only — no content DB access
