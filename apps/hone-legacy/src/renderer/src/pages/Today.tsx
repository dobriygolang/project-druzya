import type React from 'react';

export interface StartFocusArgs {
  planItemId?: string;
  pinnedTitle?: string;
}

/** Placeholder — goal tracking moved post-MVP. */
export const TodayGoalSection: React.FC = () => null;
