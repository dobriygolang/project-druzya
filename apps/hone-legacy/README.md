# Hone Legacy (Electron)

Archived Electron + electron-vite app. Replaced by [`../hone/`](../hone/) (Tauri).

Do not extend this tree for new features — copy selectively into the Tauri app.

Run (if needed):

```bash
cd apps/hone-legacy
npm install
npm run dev:electron
```
